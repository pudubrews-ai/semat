{
  "name": "semat",
  "version": "1.0.0",
  "description": "SE Maturity Assessment Tool — evaluates frontier AI model SE capability through realistic enterprise sales simulation",
  "main": "dist/orchestrator.js",
  "scripts": {
    "start": "tsx src/orchestrator.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
